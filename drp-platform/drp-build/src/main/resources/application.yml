server:
  port: 8082
  application:
    name: drp-build

spring:
  application:
    name: drp-build
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/drp?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: 1q2w3e4r
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
  data:
    redis:
      host: localhost
      port: 6379
      password: 1Q2w3e4r!
      database: 0

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  type-aliases-package: com.drp.build.entity
  global-config:
    db-config:
      id-type: AUTO
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    root: INFO
    com.drp: DEBUG
